<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
                                 http://www.springframework.org/schema/beans/spring-beans.xsd
                                 http://www.springframework.org/schema/security
                                 http://www.springframework.org/schema/security/spring-security.xsd">

    <http>
        <intercept-url pattern="/author/delete/**" access="hasRole('ADMIN')" />
        <intercept-url pattern="/author/edit/**" access="hasRole('ADMIN')" />
        <intercept-url pattern="/author/add/**" access="hasRole('ADMIN')" />
        <intercept-url pattern="/book/delete/**" access="hasRole('ADMIN')" />
        <intercept-url pattern="/book/edit/**" access="hasRole('ADMIN')" />
        <intercept-url pattern="/book/add/**" access="hasRole('ADMIN')" />
        <intercept-url pattern="/book/**" access="hasAnyRole('ADMIN', 'USER')" />
        <intercept-url pattern="/author/**" access="hasAnyRole('ADMIN', 'USER')" />
        <form-login />
        <logout />
        <remember-me />
    </http>

    <authentication-manager>
        <authentication-provider>
            <password-encoder hash="bcrypt" />
            <jdbc-user-service data-source-ref="dataSource"
                             users-by-username-query="select username,password,enabled from users where username=?"
                             authorities-by-username-query="select u.username, ur.authority from users u, user_roles ur where u.user_id = ur.user_id and u.username=?" />
        </authentication-provider>
    </authentication-manager>

</beans:beans>
